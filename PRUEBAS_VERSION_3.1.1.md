# Pruebas versión 3.1.1

- Sintaxis JavaScript validada con Node.js.
- Sintaxis de Google Apps Script validada como JavaScript después de retirar anotaciones incompatibles.
- Se verificó que `gpsFilterPayload`, `gpsFilterControls`, `gpsVehicleOptions` y sus dependencias existan.
- Se verificó la asignación de ruta sin punto operacional configurado.
- Se verificó la asignación de ruta con punto operacional usado como sugerencia editable.
- Se confirmó que iniciar y finalizar operaciones continúan llamando la validación geográfica.
- Se comprobó que el ZIP conserve todos los archivos en la raíz.
